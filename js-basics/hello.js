let emails = ["abiolla@gmail.com", "morabi@gmail.com", "ofori@gmail.com"];

for (let i = 0; i < 3; i++){
	// send email
console.log("Sending an email to ", emails[i])}
// you can replace 3 with emails.length so you'll have i < emails.length

function sendEmails(emails) {
	for (let i = 0; i < emails.length
		; i++){
        // send email
        console.log("Sending an email to ", emails[i])}
}

sendEmails(emails
)

function sayHello() {
	console.log("Hello there!")}

// below is how we call a function we've created
sayHello()
